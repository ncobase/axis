import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  authWrapper: {
    height: '100vh',
    width: '100vw',
    backgroundSize: 'cover',
    backgroundImage:
      'url(https://images.unsplash.com/photo-1484242857719-4b9144542727?auto=format&fit=crop)'
  }
}));
